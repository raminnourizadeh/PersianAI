use_default_settings: true

general:
  debug: false
  instance_name: "Persian RAG Search"

search:
  safe_search: 1
  default_lang: "all"
  formats:
    - html
    - json

server:
  secret_key: "overridden-by-SEARXNG_SECRET"
  limiter: false
  image_proxy: false
